# Notebooks

These notebooks are supplementary materials for preprocessing and modeling.

- `modeling/training.ipynb`: main machine learning comparison workflow
- `preprocessing/`: source-data preparation notebooks for crop yield tables, MODIS products, and regional aggregation

Notebook outputs have been cleared to keep the repository reviewable. Run notebooks from the repository root so paths such as `data/processed/` and `configs/search_spaces/` resolve correctly.
