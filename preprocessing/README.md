# Preprocessing Metadata

`mapping.yaml` contains crop, indicator, and Ukrainian region mappings used by the preprocessing notebooks.

The notebooks that use this metadata are kept under `notebooks/preprocessing/`. Large raw inputs and generated intermediate files are represented by the documented local layout in `DATA_POLICY.md`.
