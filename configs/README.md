# Configs

`search_spaces/` contains YAML search-space definitions used by Bayesian hyperparameter optimization.

The model names match the experiment code:

- `svm`
- `rf`
- `gb`
- `xgb`
- `dt`
- `knn`
